# Phase 86 changelog

## 86.2
- Normalise the two rank-three `RatFunc` aeval identities with `ring` after `simp`.
- Unfold rank-three numerator/denominator scalar definitions during `FractionRing.algEquiv` transport.
- No theorem statement or mathematical content changed.
