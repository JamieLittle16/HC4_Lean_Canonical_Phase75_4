# Phase 84 changelog

## Phase 84
- Added canonical `RatFunc.num`/`denom` reduced-pair bridge for source `rho = E(phi)/phi` and the explicit rank-three target.

## Phase 84.1
- Fixed `K[X]` notation-scope parsing failure in `CanonicalReducedFraction.lean`.
- Replaced scoped polynomial notation by explicit `Polynomial K` types and explicit polynomial-to-`RatFunc` algebra maps.
- No mathematical changes.
